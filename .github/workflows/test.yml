name: Test AvisaExperts Cron

on:
  schedule:
    - cron: '*/10 * * * *'  # Every 10 minutes
  workflow_dispatch:        # Allow manual trigger

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Call Test URL
        run: |
          echo "🚀 Triggering PHP file..."
          curl -s "https://avisaexperts.com/schedulecron/check.php"
          echo "✅ Request sent successfully"
